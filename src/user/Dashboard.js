import React from 'react'
import Base from '../core/Base'

export default function Dashboard() {
    return (
        <Base title="User Dashboard" description="changes a user can do !!">
            this is a user dashboard
        </Base>
    )
}
